import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * Punto de extension para Shell/Header/Sidenav (ver ANGULAR_MIGRATION_COMPLETE.md,
 * seccion "Pasos Siguientes"). Vacio en este esqueleto minimo.
 */
@NgModule({
  imports: [CommonModule],
})
export class LayoutModule {}
